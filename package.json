{
  "name": "distributed-job-queue",
  "version": "1.0.0",
  "description": "Distributed job queue on MongoDB with atomic claiming, retries, dead-lettering, and a live dashboard",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "worker": "node src/worker/worker.js",
    "test": "node --test --test-concurrency=1",
    "purge": "node scripts/purge.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.3",
    "ws": "^8.21.3",
    "zod": "^4.4.3"
  }
}
